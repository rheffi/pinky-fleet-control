# Pinky Fleet Control — 프로젝트 메인

갱신: 2026-09-16

**샘플 관제 구현·검증 완료(F1~F6).** [관제 화면](http://127.0.0.1:8080/) · [환경 확인](http://127.0.0.1:8080/environment) · [상태 API](http://127.0.0.1:8080/api/health)

기존 Docker 4개 서비스에 sample profile의 샘플 실행기를 추가했다. 로봇 3대 상태·목표·경로·작업·이벤트를 샘플로 표시하며, 실제 로봇과 알고리즘은 아직 연결하지 않았다.

## 1. 목표와 진행 원칙

실제 PinkyPro 3대가 하나의 공통 지도에서 지정된 목적지로 이동하고, 공용 통로에서 대기·통과 순서를 조정하는 모습을 Laravel + Vue 관제 화면으로 시연한다.

**진행 순서: 현재 Windows 노트북에서 Docker 개발 → Ubuntu 관제 노트북에 조기 배포 → 실물 한 대 연결 → 두 대·세 대 통합 → 인터넷 없이 시연.**

이 README가 전체 계획과 진행 상황의 기준이다. 세부 문서를 한꺼번에 만들지 않는다. 다음 작업을 시작할 때 해당 단계의 문서 하나를 작성하고, 계획·실행·검증 결과를 같은 문서에 누적한다. 이전에 작성한 상세 문서는 이전 초안이며, 현재 결정이나 완료 증거로 자동 적용하지 않는다.

## 2. 확정한 구성

| 항목 | 결정 |
|---|---|
| 개발 장소 | 현재 Windows 노트북, Docker Desktop + WSL2 기반 Linux 컨테이너 |
| 배포 장소 | 지급된 RTX 5080 노트북, Ubuntu 설치 후 Docker Engine + Compose 사용 |
| 서버·화면 | 신규 Laravel + Vue 프로젝트 |
| 데이터 저장 | MySQL 컨테이너, 데이터는 별도 볼륨으로 유지 |
| 실행 환경 | PHP·Composer·Node를 프로젝트 컨테이너에서 사용, 호스트 PHP 7.4에 맞추지 않음 |
| 알고리즘 | 팀원이 기존 Python 예제를 수정, 합의한 입출력으로 연결 |
| 로봇 | PinkyPro 실물 3대, 기존 Pinky 패키지·Nav2 활용 |
| 현장 네트워크 | 전원만 연결한 공유기에 Ubuntu 노트북과 로봇 3대 연결, 인터넷 없이 운영 |
| 연결 방향 | 내부 IP를 이용한 로봇 HTTP API 접근부터 검증, 실행 제어 보완 방식은 담당자와 합의 |
| 공유 | Git으로 코드·실행 설정 공유, 오프라인 배포용 이미지는 별도로 전달 |
| 지도 | 제작한 폼 미로의 실제 SLAM 지도 중 공통 지도 선택 |

현재 개발환경은 Laravel 13.32.0·PHP 8.4.25·Vue 3.5.42·Node 24.21.0·MySQL 8.4.11로 검증했다. 상세 버전·이미지·실행 방법은 개발환경 문서에 기록했다. 별도 5080 노트북의 Ubuntu 준비·배포 여부와 구분한다.

## 3. 시스템 구조

```text
현재 Windows 개발 노트북
  Docker: Laravel + Vue 개발·빌드 + MySQL
  샘플 데이터로 API·관제 화면 확인
                 │
       Git 코드 공유 / 빌드 이미지 전달
                 ▼
5080 Ubuntu 관제 노트북
  Docker: Laravel + 빌드된 Vue + MySQL
  팀원 알고리즘 연결 / 실제 로봇 실행 관리
                 │
        공유기 내부망 / HTTP API
       ┌─────────┼─────────┐
     eed0       648d      62b2
   Domain 35  Domain 30  Domain 40
     각 로봇: 위치 추정·Nav2·모터 제어
```

알고리즘과 실행 관리 프로그램의 프로세스 구성·배치 위치는 담당자와 합의한다. ROS2가 필요한 부분은 담당자가 검증한 환경에서 먼저 연결한다. Laravel은 작업·명령·상태를 관리하며, Vue나 PHP 웹 요청에서 모터 제어 루프를 돌리지 않는다.

## 4. 시연 범위

### 필수 시나리오: 공용 통로를 이용하는 로봇 3대

1. 세 로봇을 지정된 시작 위치에 놓고 공통 지도에서 위치 추정을 확인한다.
2. 관제에서 미리 정한 고정 목적지 시나리오를 선택한다.
3. 로봇별 목적지와 알고리즘의 계획 경로·대기를 표시한다.
4. 실행하면 서로 간섭하지 않는 구간에서 함께 이동한다.
5. 공용 통로에서는 실제 진행 상태를 확인하며 대기·진입·통과를 관리한다.
6. 목적지 도착 결과와 소요 시간·대기·오류 기록을 표시한다.

시작점·목표는 공통 지도 확정 후 고른다. 좁은 통로를 막는 위치에 목적지를 두지 않고, 대기·통과 공간을 확인한다. 구체적인 충돌 처리와 진행 허가 방식은 알고리즘·실행 담당자가 함께 검증한다.

### 최소 관제 기능

- 로봇 3대의 연결 상태, 마지막 상태 수신 시각, 현재 위치.
- 공통 지도, 고정 목적지, 계획 경로와 실제 위치 표시.
- 작업 시작, 전체 정지 요청, 작업 결과 조회.
- 이동·대기·도착·오류 상태와 작업 로그.
- 샘플 모드와 실제 로봇 모드 구분.

추가 범위는 필수 시연 완료 후 시작점·목적지 자동 배정이다. YOLO/OpenCV 기능은 현재 필수 범위에 포함하지 않는다.

## 5. 단계별 작업과 완료 기준

상태는 `대기 / 진행 / 완료 / 확인 필요`로 기록한다. 완료는 실제 확인한 결과가 있을 때만 표시한다.

| 단계 | 작업 | 완료 기준 | 현재 상태 |
|---|---|---|---|
| 1 | 현재 노트북 Docker·WSL2 확인, 버전 선정, Laravel·Vue·MySQL 구성 | 화면 열림, API 응답, DB 저장·재시작 후 유지 확인 | 완료 — E1~E7 및 결과 기록 완료 |
| 2 | 샘플 데이터로 최소 관제 API·화면 구현 | 3대 표시, 목표 선택, 작업·상태 흐름 확인 | 완료 — F1~F6, PHP 17개·JS 4개·빌드·실행기/전체 서비스 재시작 확인 |
| 3 | Ubuntu·Docker 준비, 최소 관제 조기 배포, 한 대 API 연결 | Ubuntu 관제에 실제 로봇 상태 표시 | 진행 — 공식 Docker와 운영 관제 배포·DB·HTTP·복구 검증 완료, 재부팅·실제 인터넷 차단·로봇 연결 대기 |
| 4 | 공통 지도 등록, 좌표 검증, 한 대 고정 목표 주행 | 관제에서 시작해 실제 도착·정지 확인 | 진행 — 1cm 지도·신규 Nav2 번들 설치 및 Domain 40 실행값 확인, 번들 README의 좁은 슬롯 ABORT 이슈는 미해결 |
| 5 | 팀원 알고리즘 연결, 경로·대기의 실제 실행 처리 | 한 대가 계획 단계·대기를 따르는지 대조 | 대기 |
| 6 | 두 대 검증 후 세 대 통합 | 공용 구간 대기·통과, 지연·정지 처리 확인 | 대기 |
| 7 | 오프라인 재부팅·반복 시연, 결과 기록 | 인터넷 없이 배포된 구성으로 시연 재현 | 대기 |

현재 노트북에서 1~2단계를 진행하는 동안 다른 팀원은 Ubuntu 설치, 공통 지도 선정, 로봇 API 확인을 병행한다. 관제 전체 기능이 완성되기 전에 3단계에서 연결한다.

## 6. 팀 역할과 인계 항목

| 담당 | 맡을 일 | 필요한 인계 |
|---|---|---|
| 본인 | Docker 개발 구성, Laravel API·작업 관리, Vue 관제 화면, 배포 구성 | API 입력·출력, 실행 방법, 검증 결과 |
| 알고리즘 담당 1명 | 3대 경로 계획, 경로·대기 출력, 예제 수정 | 입출력 샘플, 좌표·시간 의미, 실행 환경·실패 응답 |
| 나머지 4명 | 지도·좌표, 로봇 API, Nav2·실행 제어, Ubuntu·네트워크·통합 시험 분담 | 공통 지도, IP·포트, 실제 상태·도착·정지 정보, 시험 결과 |

나머지 4명의 세부 담당 배정은 팀에서 정한다. 팀원 알고리즘을 관제 작업에서 임의로 수정하지 않는다.

## 7. 실제 주행에서 반드시 맞출 사항

- **지도:** 실제 지도 이미지와 YAML의 해상도·원점·방향을 기준으로 한다. 이전 25cm 격자·예시 크기를 실제 벽 좌표로 하드코딩하지 않는다.
- **차체:** 사용자 제공 크기는 약 가로 12cm·깊이 14cm·높이 16cm다. 회전 공간·위치 오차를 포함한 통과 여유는 실측한다.
- **계획과 실행:** CBS 예제의 충돌 없는 계획만으로 실물 충돌 방지가 보장되지는 않는다. Nav2에 최종 목적지만 전달했을 때 경로·대기가 보존되는지 별도 확인한다.
- **속도와 지연:** 이동·회전 시간 차이, 속도 제한, 경로 이탈에 대해 대기·진행 허가·재계획 정책을 정한다.
- **결과:** HTTP 요청 접수와 실제 출발·도착·정지를 구분한다. 상태 값 하나가 false라는 이유로 도착이나 정지를 확정하지 않는다.
- **통신 실패:** 상태가 오래됐거나 연결이 끊기면 다음 구간 진입을 보류하고, 로봇 측 정지·복구 동작을 시험한다. 통신이 끊겼을 때 관제의 정지 요청만으로 멈출 수 있다고 가정하지 않는다.

## 8. 인터넷 없는 현장 배포

공유기 내부 통신에는 인터넷이 필요하지 않다. 장비 간 통신 허용, IP 할당·주소 예약, API 수신 주소, 방화벽·Docker 포트 설정을 확인한다. IP와 포트는 아직 미확정이며, ROS Domain 번호와 구분한다.

배포 순서:

1. 인터넷이 되는 환경에서 Ubuntu·Docker 설치와 필요한 의존성 준비를 마친다.
2. 현재 노트북에서 배포 대상 CPU 구조에 맞는 Linux 이미지를 빌드한다. 앱 의존성과 Vue 빌드 결과를 포함하고 MySQL 등 필요한 이미지도 함께 준비한다.
3. 코드와 설정은 Git으로 공유한다. 오프라인 현장에는 Docker 이미지 파일, Compose 설정, 지도 파일을 USB 또는 내부망으로 전달한다.
4. Ubuntu에서 이미지를 불러오고, 현장 환경변수·DB 볼륨·앱 키·DB 초기화를 설정한다. 이미지 전달은 DB 데이터 백업을 대신하지 않는다.
5. 현장에서는 외부 다운로드 없이 실행되도록 구성한다. 외부 CDN·폰트·클라우드 API 의존성을 제거하고, Gurobi를 계속 쓰면 라이선스의 오프라인 동작을 확인한다.
6. 인터넷을 끊고 재부팅한 뒤 관제 실행·로봇 3대 상태 조회·주행을 검증한다.

개발은 코드 수정이 바로 반영되게 구성하고, 시연은 빌드된 화면과 버전을 고정한 이미지로 실행한다. Wi-Fi 비밀번호·토큰·실제 환경변수는 Git과 문서에 저장하지 않는다.

## 9. 현재 확인한 상황

| 항목 | 상태·근거 |
|---|---|
| 프로젝트 방향 | Docker 개발, Ubuntu 배포, 내부망 시연으로 합의 |
| 폼 미로·공유기·로봇 3대 | 준비됨 — 사용자 보고 |
| 공통 지도 | 2026-09-16 20:07 교체본 147×207 px, 0.01m/px, 원점 `[-0.338, -1.761, 0]`, 버전 `b82b34d23d60`. 관제 PNG·env·운영 이미지 재빌드·반출 tar 반영 완료, 로봇 지도 동기화·실제 코스 일치 검증 대기. 이전본은 `map/cbs_map_bk*.pgm` |
| 현재 노트북 | Windows 11 Pro. Docker Desktop 4.43.1·CLI/Server 28.3.0·Compose v2.38.1. Linux 엔진 연결 및 hello-world 다운로드·실행 성공. 기존 PHP 7.4는 유지 |
| WSL2 | WSL 2.5.9.0. Ubuntu-24.04(기본)·Ubuntu-22.04·docker-desktop 모두 VERSION 2 등록 확인. 최초 조회 시 정지 상태였고, 이후 Docker Linux 엔진 실행 검증 완료 |
| 5080 노트북 | Ubuntu 24.04.5 LTS·x86_64. 공식 Docker 29.8.1·Compose 5.5.1 설치, 운영 web·app·mysql 배포와 migration·seed·HTTP·DB 보존·Docker 데몬 복구 검증 완료. ROS 2 Jazzy Desktop·Nav2, Pinky Studio 0.2.3 설치, 로컬 DDS 시험, 공식 PinkyPro 워크스페이스와 프로젝트 지도·파라미터 빌드 완료. Domain 40 실물 로봇 연결 대기 |
| 앱 개발환경 | Laravel·Vue·MySQL 실행, API·DB·HMR·재시작 데이터 유지·배포 빌드 확인. 자동 테스트 4개 통과 |
| 샘플 관제 | 3대 상태·목표·경로·작업·이벤트 구현. 정상 완료·정지, 동시 요청·재요청, 통신 실패·실행기 재시작·데이터 유지, PHP/JS 테스트·빌드 확인 |
| 알고리즘 통합·실물 시험 | 신규 1cm 지도와 Nav2 파라미터가 Domain 40에서 실제 로드되고 lifecycle 노드가 active인 것까지 확인. 번들 제공자의 좁은 슬롯 ABORT 문제는 미해결 |
| 팀 Git 저장소 | 비공개 `rheffi/pinky-fleet-control`, `main` 게시·추적 설정 완료 |

## 10. 문서 작성과 다음 작업

**Ubuntu 관제 웹 배포와 ROS 관제 PC 준비를 완료했다.** 다음 단계는 `62b2`를 같은 Wi-Fi의 Domain 40에 연결해 실제 노드·토픽·액션을 수집하고, 그 결과로 상태 어댑터 인터페이스를 확정하는 것이다.

검증 중 테스트 DB 격리 오류로 기존 개발 DB의 샘플 작업 이력·환경 확인값이 초기화됐다. 테스트 bootstrap에서 모든 환경변수 출처를 SQLite 메모리 DB로 고정하고, migration 전에 잘못된 DB 연결을 거부하도록 수정했다. 이전 이력은 복구하지 못했으며 이후 새 샘플 데이터로 테스트·재시작 보존을 확인했다. 상세 기록은 02 문서 5.4에 남긴다.

세부 문서는 필요할 때 한 개씩 만들며 아래 순서로 기록한다.

1. 목표와 이번 작업 범위.
2. 확인한 환경·결정 사항·남은 질문.
3. 실행할 작업과 완료 기준.
4. 실제 명령·변경 파일·결과.
5. 검증 결과·미해결 문제·다음 작업.

작업 후에는 이 README의 단계 상태와 아래 로그를 갱신한다. 샘플 시험, 코드 검사, 실제 로봇 시험을 구분해서 기록한다.

### 현재 단계 문서

[06. 오프라인 공유기 현장 통합 시험 시나리오](docs/06-offline-router-field-test.md) — 인터넷 없는 공유기에 관제 노트북·로봇 3대 연결, `localhost:8080` 관제로 순차 주행·정지·통신 끊김·재부팅 복구 시험(C1~C10, F1~F10). 시나리오 작성만 완료, 시험 미수행.

[05. 관제서버 상황 보고 테스트 시나리오](docs/05-status-reporting-test.md) — 실행기 보고 → API·DB·화면 전달 검증. 자동(A)·가짜 실행기(B)·실물(C) 수준별 S0~S19와 정적 검토 위험 R1~R7. 시나리오 작성만 완료, 시험 미수행.

[04. Ubuntu 관제서버 배포 준비와 인계](docs/04-ubuntu-control-server.md) — Git 인계 절차, Ubuntu 24.04 운영 Docker 배포 순서·완료 기준, Ubuntu Codex 전달 프롬프트.

[03. 단일 로봇 Nav2 주행 안정화 테스트](docs/03-single-robot-navigation-test.md) — Domain 40 로봇 시험 설정, AMCL 수렴 기준, 직선·코너·웨이포인트 단계별 시나리오. 설정 파일 반영 완료, Nav2 재시작과 실물 시험 결과 기록 대기.

[02. 샘플 관제 — 데이터 규약과 구현 계획](docs/02-sample-fleet-control.md) — F1~F6 결과·실행 명령·검증 중 수정 기록. 외부 로봇·알고리즘 인터페이스는 담당자 합의가 필요하다.

[01. 개발환경 — 상태 확인과 실행 계획](docs/01-development-environment.md)

이 문서에 E1~E7 검증 결과와 E8의 버전·실행 방법 기록을 완료했다. 현재 폴더에서는 `docker compose up -d --wait`로 개발환경을 시작한다. DB 데이터는 Docker 볼륨에 저장한다.

### 실행 기록

| 날짜 | 수행 내용 | 결과 |
|---|---|---|
| 2026-09-16 | 로봇 실행기 3개 실행 | 새 지도 버전 env로 62b2(D40)·648d(D30)·eed0(D35) 실행기를 백그라운드 실행, 로그 `~/pinky-fleet-agent-logs/`. 앱 토큰 일치, telemetry·command 요청 모두 HTTP 200 확인. 로봇이 같은 네트워크에 없어 세 대 모두 offline 표시(정상). 로봇 지도는 사용자 확인으로 복사 생략 |
| 2026-09-16 | 새 지도로 운영 이미지 재빌드·검증 | `up --build --wait`로 app·web 재생성, 세 서비스 healthy·DB 유지. bootstrap 지도 `b82b34d23d60`·147×207·원점 `[-0.338,-1.761,0]`, 제공 PNG가 원본과 바이트 일치, app 컨테이너 env 버전 일치. `artifacts/` 이미지 tar(470MB)·SHA-256 재생성 검증. PHP 테스트 14개(107 assertions) 통과. 실행기는 실행 중이 아니었고, 로봇 IP 미확인으로 로봇 지도 복사는 미수행 |
| 2026-09-16 | 공통 지도 교체 반영 | 새 `cbs_map.pgm`(147×207, `b82b34d23d60`)에서 `public/maps/cbs_map.png` 재생성, yaml `image`를 `cbs_map.pgm`으로 수정, `.env`·`deploy/production.env` 지도 버전 갱신, FleetTest 지도 크기 기대값 수정. 운영 이미지 재빌드·테스트 실행·로봇 지도 복사는 미수행 |
| 2026-09-16 | 오프라인 공유기 현장 통합 시험 시나리오 작성 | 06 문서 추가. 현재 관제는 한 번에 한 대만 주행 가능하므로 3대 순차 시연 기준으로 구성. 공유기 AP 격리·멀티캐스트, 로봇 시각, 목표 좌표 실측·env 반영, `--pull never` 기동 절차 포함. 시험 미수행 |
| 2026-09-16 | 관제서버 상황 보고 테스트 시나리오 작성 | 05 문서 추가. 코드 정적 검토로 신선도·도착·정지 판정 규칙 정리, AMCL 중단 시 online 유지(R1)·보고 끊긴 작업 무기한 활성(R4)·지도 범위 미검사(R6) 등 확인 필요 사항 도출. 시험·코드 변경 미수행 |
| 2026-09-16 | 관제 환경변수 구성 | Git에서 제외된 루트·운영 `.env`를 권한 600으로 작성하고 현재 지도 해시, 지도 프레임, 세 로봇 Domain을 연결. 초기·목표 좌표는 실측 전 빈 값으로 두고 설정에서 `null`로 처리 |
| 2026-09-16 | 관제 화면 공통 지도 교체 | `cbs_map.pgm`을 무회전·무반전 PNG로 변환하고 YAML의 해상도·원점과 PNG 크기를 `display_map` API로 제공. 샘플 명령 지도와 실제 표시 지도를 분리한 채 운영 app·web 재배포, 이미지·API HTTP 200과 세 서비스 healthy 확인 |
| 2026-09-16 | Ubuntu ROS 2·PinkyPro 관제 PC 준비 | ROS 2 Jazzy Desktop·Nav2 설치, Fast DDS 로컬 송수신 성공. 공식 PinkyPro `75f76e8` clone 후 프로젝트 Nav2 파라미터·1cm 지도 반영, `pinky_navigation` 빌드와 source/install 해시 일치 확인. Pinky Studio 0.2.3 설치와 Bluetooth 준비 완료, Domain 40 실물 로봇 토픽은 아직 미발견 |
| 2026-09-16 | Ubuntu 공식 Docker 설치와 운영 관제 배포 | 빈 Snap Docker 제거 후 공식 Engine 29.8.1·Compose 5.5.1 설치, hello-world 성공. 운영 이미지 빌드, migration·FleetSeeder, 세 서비스 healthy, 화면·API·자산 200 확인. 컨테이너·Docker 데몬·로컬 이미지만 사용한 재생성 뒤 DB 유지 확인. 실제 인터넷 차단·호스트 재부팅·다른 기기 접속은 대기 |
| 2026-09-16 | Ubuntu 운영용 컨테이너 구성 작성 | web·app·mysql 전용 Compose, 다단계 app/web 이미지, Nginx·PHP 운영 설정, 환경변수 템플릿 추가. 당시 Docker 권한으로 빌드·기동이 대기됐고, 후속 공식 Docker 설치 작업에서 완료 |
| 2026-09-16 | Ubuntu 관제 노트북 초기 점검·세팅 계획 구체화 | OS·자원·Git·네트워크·포트·Docker·ROS 상태 확인. Snap Docker 데이터 확인 → 공식 Docker 준비 → 운영 배포 → 복구 검증 → Domain 40 단일 로봇 연결의 게이트를 04 문서에 기록. 설치·서비스 변경은 미수행 |
| 2026-09-16 | 팀 GitHub 저장소 생성·게시 | 비공개 `rheffi/pinky-fleet-control` 생성, 로컬 `origin` 연결, `main` push·upstream 설정 완료 |
| 2026-09-16 | Ubuntu 24.04 관제서버 Git 인계 준비 | 04 문서에 Git·운영 Docker·오프라인·단일 로봇 연결 순서와 Ubuntu Codex 전달 프롬프트 작성. 로컬 main 첫 커밋·추적 제외·비밀 패턴 검사 및 오프라인 Git bundle 검증 완료 |
| 2026-09-16 | 수령한 1cm 지도·Nav2 번들 적용 상태 확인 | 로봇의 src/install 파라미터와 로컬 수령 파일 SHA-256 일치. 실행 지도 140x205@0.01m, local/global inflation 0.12·scaling 3.0·padding 0.01, 주요 lifecycle 노드 active 확인. 번들 자체의 좁은 슬롯 ABORT 이슈는 미해결 |
| 2026-09-16 | Domain 40 단일 로봇 주행 안정화 설정·시험 시나리오 작성 | Pinky 설치 파라미터에 global inflation 0.15/3.0, planner tolerance 0.10·unknown 차단, AMCL 갱신 0.02m/0.05rad·수동 초기 위치를 반영하고 원격 백업 생성. 현재 실행 노드는 이전 값을 사용 중이므로 Nav2 재시작·T0~T7 실물 검증 대기 |
| 2026-09-16 | 컨테이너 재시작 후 Nginx 502 수정 | Nginx가 Docker DNS를 1초 주기로 다시 조회하도록 변경. app IP를 172.19.0.3에서 172.19.0.6으로 바꾸고 web 재시작 없이 API·DB 정상 응답 확인 |
| 2026-09-16 | F5~F6 최종 검증·문서화 | 테스트 DB 격리 오류로 샘플 이력 초기화 발생 후 차단 장치 추가. PHP 17개(94 assertions)·JS 4개·Pint·빌드 통과. 실행기 지연/재시작/정지·전체 서비스 재시작 후 새 작업·이벤트 유지 확인 |
| 2026-09-16 | F1~F4 샘플 관제 구현 및 F5 일부 검증 | 6개 테이블·7개 API·샘플 CLI·Vue 관제 추가. 브라우저 완료·정지, HTTP 동시 시작 201/409·재요청 200·정지 접수 202·입력/CSRF/출처 검사 확인. 컨테이너 테스트·빌드·재시작 검증은 권한 복구 후 진행 |
| 2026-09-16 | 샘플 관제 계획·데이터 규약 작성 | 02 문서 하나 추가, API·상태·샘플 실행기·Vue 화면·검증 기준 정리. 기능 코드 변경·시험 미수행 |
| 2026-09-16 | 사용자 합의에 따라 메인 README 재작성 | Docker 개발·Ubuntu 배포·오프라인 시연과 단계별 문서 운영 기준 정리. 설치·앱 구현·로봇 시험은 미수행 |
| 2026-09-16 | Docker·WSL2 상태 확인 및 개발환경 문서 한 개 작성 | 설치 확인, Docker 엔진 정지 상태 판단. 3306 점유를 고려해 MySQL 내부 포트만 사용하는 구성 계획. 설치·기동·앱 생성은 미수행 |
| 2026-09-16 | E1·E2 Docker 실행 검증 | 사용자 권한 변경 후 Server 28.3.0·linux/x86_64 확인. hello-world 다운로드·실행 종료 코드 0, 샘플 컨테이너 자동 제거 확인. 다음 E3 |
| 2026-09-16 | E3 Docker 개발 설정 작성·검증 | Compose 4개 서비스·내부 DB·전용 볼륨 구성. Compose 모델·Dockerfile 정적 검사·Nginx 문법 검사 통과. 실제 앱 이미지 빌드와 앱 생성은 E4에서 진행 |
| 2026-09-16 | E4~E7 실행 및 결과 기록 | Laravel·Vue 설치, MySQL 마이그레이션, 화면·API·HMR 검증, 전체 서비스 재시작 후 데이터 유지, 자동 테스트 4개·Pint·Vite 빌드 통과. 남은 public/hot 문제 수정 후 개발 서버 없이 화면 확인. 개발용 4개 서비스 실행 중 |

## 참고

- [PinkyPro 공식 저장소](https://github.com/pinklab-art/pinky_pro)
- [Docker Engine Ubuntu 설치](https://docs.docker.com/engine/install/ubuntu/)
- [Windows Docker WSL2 구성](https://docs.docker.com/desktop/features/wsl/)
- [Docker Compose 배포 구성](https://docs.docker.com/compose/how-tos/production/)
- [Docker 이미지 내보내기](https://docs.docker.com/reference/cli/docker/image/save/) · [가져오기](https://docs.docker.com/reference/cli/docker/image/load/)
